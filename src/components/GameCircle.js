import React from "react";
import '../Game.css';

const GameCircle = ({id}) => {
    console.log(id);
    return (
        <div className="gameCircle">
            GameCircle
        </div>
    )
}

export default GameCircle;